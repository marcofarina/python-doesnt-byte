// Albero sidebar UNICO del Manuale del Programmatore (Volume 1 — 3a).
// Unione dei contenuti dei vecchi percorsi it/liceo/its. Fonte unica sia per
// la sidebar Docusaurus (sidebars/programmatore.ts) sia per il plugin
// curriculum (manifest + configuratore). CJS: require()-abile da entrambi.
module.exports = ['intro', 'variabili'];
